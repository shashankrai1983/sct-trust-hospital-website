# Test Environment Variables
# Copy from .env.local and modify as needed for testing
# WARNING: Never use production credentials in test files

# Admin credentials for testing - CHANGE THESE VALUES
ADMIN_EMAIL=admin@scttrusthospital.com
TEST_ADMIN_PASSWORD=test-password-change-me

# NextAuth - Generate test secret
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=test-secret-generate-new-one-for-production

# MongoDB (use test database)
MONGODB_URI=mongodb://localhost:27017/sct-trust-hospital-test
MONGODB_DB=sct-trust-hospital-test

# Admin password hash (for bcrypt comparison tests)
# Generate with: node scripts/generate-admin-hash.js
ADMIN_PASSWORD_HASH=generate-valid-hash-for-test-password

# Test configuration
TEST_BASE_URL=http://localhost:3000
TEST_TIMEOUT=30000

# reCAPTCHA (use test keys for development)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=test-site-key-here
RECAPTCHA_SECRET_KEY=test-secret-key-here